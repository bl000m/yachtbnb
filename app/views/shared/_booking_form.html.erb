<div class="container">
  <div class="form-wrapper">
    <h2>Book your yacht</h2>
    <%= simple_form_for [@yacht, @booking] do |f| %>
      <%= f.input :voyager, label: "nombre de loup de mer:",
                             as: :integer,
                             required: false %>
      <%= f.input :start_date, label: "Sélectionnez vos dates:",
                               as: :string,
                               required: false,
                               input_html: { id: "range_start", class: "d-none"} %>
      <%= f.input :end_date, label: false,
                             as: :string,
                             required: false,
                             input_html: { id: "range_end", class: "d-none" } %>
      <%= f.button :submit, "Book", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
